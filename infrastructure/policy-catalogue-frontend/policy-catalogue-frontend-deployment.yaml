apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-catalogue-frontend
spec:
  selector:
    matchLabels:
      app: policy-catalogue-frontend
  replicas: 2
  template:
    metadata:
      labels:
        app: policy-catalogue-frontend
    spec:
      containers:
        - name: policy-catalogue-frontend
          image: policy-catalogue-frontend/latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          env:
            - name: ENV
              value: "dev"
          envFrom:
            - configMapRef:
                name: service-addresses-configmap
