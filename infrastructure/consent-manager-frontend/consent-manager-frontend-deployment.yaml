apiVersion: apps/v1
kind: Deployment
metadata:
  name: consent-manager-frontend
spec:
  selector:
    matchLabels:
      app: consent-manager-frontend
  replicas: 2
  template:
    metadata:
      labels:
        app: consent-manager-frontend
    spec:
      containers:
        - name: consent-manager-frontend
          image: consent-manager-frontend/latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          env:
            - name: ENV
              value: "dev"
          envFrom:
            - configMapRef:
                name: service-addresses-configmap
