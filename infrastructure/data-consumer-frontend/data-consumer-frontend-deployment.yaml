apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-consumer-frontend
spec:
  selector:
    matchLabels:
      app: data-consumer-frontend
  replicas: 2
  template:
    metadata:
      labels:
        app: data-consumer-frontend
    spec:
      containers:
        - name: data-consumer-frontend
          image: data-consumer-frontend/latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          env:
            - name: ENV
              value: "dev"
          envFrom:
            - configMapRef:
                name: service-addresses-configmap
