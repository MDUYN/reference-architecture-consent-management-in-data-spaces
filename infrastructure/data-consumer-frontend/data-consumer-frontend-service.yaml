apiVersion: v1
kind: Service
metadata:
  name: data-consumer-frontend-service
spec:
  selector:
    app: data-consumer-frontend
  ports:
    - protocol: "TCP"
      port: 3000
      targetPort: 3000
  type: LoadBalancer