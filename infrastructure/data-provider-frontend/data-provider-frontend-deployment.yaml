apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-provider-frontend
spec:
  selector:
    matchLabels:
      app: data-provider-frontend
  replicas: 2
  template:
    metadata:
      labels:
        app: data-provider-frontend
    spec:
      containers:
        - name: data-provider-frontend
          image: data-provider-frontend/latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          env:
            - name: ENV
              value: "dev"
          envFrom:
            - configMapRef:
                name: service-addresses-configmap
